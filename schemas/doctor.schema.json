{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/ShortArrow/pathlint/main/schemas/doctor.schema.json",
  "title": "Diagnostic",
  "description": "One PATH-hygiene problem reported by `pathlint doctor`. See PRD §7.7 for the full list of diagnostic kinds.",
  "type": "object",
  "oneOf": [
    {
      "type": "object",
      "required": [
        "first_index",
        "kind"
      ],
      "properties": {
        "first_index": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "kind": {
          "type": "string",
          "enum": [
            "duplicate"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "missing"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "kind",
        "suggestion"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "shortenable"
          ]
        },
        "suggestion": {
          "type": "string"
        }
      }
    },
    {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "trailing_slash"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "canonical",
        "kind"
      ],
      "properties": {
        "canonical": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "enum": [
            "case_variant"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "short_name"
          ]
        }
      }
    },
    {
      "type": "object",
      "required": [
        "kind",
        "reason"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "malformed"
          ]
        },
        "reason": {
          "type": "string"
        }
      }
    },
    {
      "description": "Multiple sources that should not coexist in PATH have all fired at once. `diagnostic` is the snake_case label identifying the specific conflict (e.g. `mise_activate_both`) and comes from a `Relation::ConflictsWhenBothInPath`. Each element of `groups` lists the PATH entries that matched the corresponding source in the relation's `sources` array. `Diagnostic.index` points at the first entry of the first non-empty group for sort stability.",
      "type": "object",
      "required": [
        "diagnostic",
        "groups",
        "kind"
      ],
      "properties": {
        "diagnostic": {
          "type": "string"
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "integer",
              "format": "uint",
              "minimum": 0.0
            }
          }
        },
        "kind": {
          "type": "string",
          "enum": [
            "conflict"
          ]
        }
      }
    },
    {
      "description": "A `[source.<name>]` declared in the merged catalog points at a per-OS path that does not exist on the filesystem. Common when a user's `pathlint.toml` declares a source for a tool they don't actually have installed (e.g. `[source.cargo] unix = \"$HOME/.cargo/bin\"` on a host without rust). 0.0.18+. `entry` is the expanded path that was checked; `Diagnostic.index` is fixed at `usize::MAX` because the diagnostic is per-source, not per-PATH-entry.",
      "type": "object",
      "required": [
        "kind",
        "source"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "per_source_missing_required"
          ]
        },
        "source": {
          "type": "string"
        }
      }
    },
    {
      "description": "Same command basename exists as a real executable in two or more PATH dirs. The earlier dir wins (`Diagnostic.index`); later dirs are shadowed (`shadowed_indexes`). Reported regardless of whether the dirs are named sources — duplicates are facts. Pairs with the relation-driven `Conflict` detector (e.g. `mise_activate_both`): that one covers the named-source-pair angle, this one covers the unnamed command-name angle. Windows compares case-insensitively after stripping PATHEXT extensions, so `python.exe` and `python.bat` count as the same command. 0.0.19+.",
      "type": "object",
      "required": [
        "command",
        "kind",
        "shadowed_indexes"
      ],
      "properties": {
        "command": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "enum": [
            "duplicate_but_shadowed"
          ]
        },
        "shadowed_indexes": {
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          }
        }
      }
    },
    {
      "description": "PATH entry resolves to a relative path (`.`, `./bin`, bare `bin`, …). The shell resolves these against the current working directory at command-invocation time, so the binary that runs depends on where the user happens to be — almost always a security or portability footgun. Env vars are expanded before the check, so `$HOME/bin` does not fire when `HOME` is set; an unresolved `$VAR/bin` stays verbatim and fires (it is itself a config bug). 0.0.20+.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "relative_path_entry"
          ]
        }
      }
    },
    {
      "description": "PATH entry resolves to a directory that is writable by users other than the owner. An attacker with shell access could drop a malicious binary that the user runs by typing a common command name. On Unix, \"writable by others\" means the others-write bit (`mode & 0o002`) is set. On Windows, the directory's DACL grants the \"Everyone\" SID effective `FILE_GENERIC_WRITE` or `FILE_APPEND_DATA`. Env vars are expanded first; missing or unreadable directories are skipped (the `Missing` detector covers them). 0.0.21+.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "writeable_path_dir"
          ]
        }
      }
    },
    {
      "description": "Selfcheck: the running pathlint binary's absolute path was not found anywhere on PATH. Either invoked by absolute path or PATH was unset. 0.0.34+ (ADR-0028).",
      "type": "object",
      "required": [
        "kind",
        "running"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "binary_not_in_path"
          ]
        },
        "running": {
          "type": "string"
        }
      }
    },
    {
      "description": "Selfcheck: a `pathlint.toml` was located but failed to parse. `reason` carries the parser's error string. 0.0.34+.",
      "type": "object",
      "required": [
        "config_path",
        "kind",
        "reason"
      ],
      "properties": {
        "config_path": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "enum": [
            "config_parse_error"
          ]
        },
        "reason": {
          "type": "string"
        }
      }
    },
    {
      "description": "Selfcheck: no `pathlint.toml` found via the standard search (cwd then `$XDG_CONFIG_HOME`). Info severity because running without a config is legitimate. 0.0.34+.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "config_not_found"
          ]
        }
      }
    },
    {
      "description": "Selfcheck: a required env var (`PATH`, `PATHEXT` on Windows, `HOME`/`USERPROFILE`) returned `None`. `entry` field carries the var name. 0.0.34+.",
      "type": "object",
      "required": [
        "kind",
        "var"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "env_lookup_failed"
          ]
        },
        "var": {
          "type": "string"
        }
      }
    }
  ],
  "required": [
    "entry",
    "index",
    "severity"
  ],
  "properties": {
    "entry": {
      "type": "string"
    },
    "index": {
      "type": "integer",
      "format": "uint",
      "minimum": 0.0
    },
    "severity": {
      "$ref": "#/definitions/Severity"
    }
  },
  "definitions": {
    "Severity": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "warn",
            "error"
          ]
        },
        {
          "description": "Informational; does not affect exit code. 0.0.34+ for the `config_not_found` selfcheck (running pathlint without a config is legitimate).",
          "type": "string",
          "enum": [
            "info"
          ]
        }
      ]
    }
  }
}
